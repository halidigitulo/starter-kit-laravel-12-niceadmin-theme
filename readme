Cara install proyek Starter Kit Laravel 12
------------------------------------
1. clone dari github: git clone https://github.com/halidigitulo/starter-kit-laravel-12.git
2. masuk ke direktori proyek: `cd starter-kit-laravel-12`
3. pada app > providers > AppServiceProvider comment baris profile agar tidak terbaca
4. install dependensi: `composer install`
5. buat file `.env` dari `.env.example`: `cp .env.example .env`
6. sesuaikan konfigurasi database pada file `.env` sesuai dengan database yang digunakan.
7. generate key aplikasi: `php artisan key:generate`
8. migrasi database: `php artisan migrate`
9. seeder database: `php artisan db:seed`
10. jalankan server: `php artisan serve`